# Build artifacts
bin/
coverage/
*.out
*.test
*.prof
mem.out
cpu.out

# Go workspace files (uncommitted)
go.work
go.work.sum

# Editor & OS
.idea/
.vscode/
.DS_Store
*.swp
*.swo

# Local environment
.env
.env.local

# Mock binary build output
test/mockserver/bin/

# Stray go-build artifacts if `go build ./test/mockserver` is run without -o
# (the binary gets dumped at cwd named after the package dir)
/mockserver
/grok-mock
/main

# Trace exports
trace-exports/

# asciinema intermediates from scripts/record-demo-gif.sh
# (final docs/gif/*.gif outputs are committed)
docs/gif/*.cast
